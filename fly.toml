app = "repo-chat-bot"
primary_region = "fra"

[build]

[env]
  REPO_PATH = "/app/repo"

[[mounts]]
  source = "repo_data"
  destination = "/app/repo"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory = "256mb"

[deploy]
  strategy = "immediate"
